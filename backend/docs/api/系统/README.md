# 系统 API

本目录只保存不属于业务页面的系统接口。

## 接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 检查后端服务是否正常，返回统一响应包体；正式替代 `/api/test` |

健康检查不要求 `X-Teacher-Id`。

Day3 联合任务直接由微信小程序请求本接口，不再新增只返回 Hello World 的重复接口。
