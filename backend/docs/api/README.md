# V2 页面 API 目录

本目录按照前端一级页面组织 V2 API：

```text
api/
├─ 随班记录/
├─ 训练计划/
├─ 学生评估/
├─ 我的/
└─ 系统/
```

OpenAPI 0.5.0、Java 实现和正式数据库已经切换到 V2。Java 代码使用相同的页面模块目录，5 个数据库集成测试场景覆盖全部 24 个接口。接口统一挂载在 `/api` 前缀下，并统一返回 `{ code, message, data }` 包体。
