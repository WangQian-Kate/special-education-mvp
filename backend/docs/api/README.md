# V2 页面 API 目录

本目录按照前端一级页面组织 V2 API：

```text
api/
├─ 登录认证/
├─ 随班记录/
├─ 训练计划/
├─ 学生评估/
├─ 我的/
└─ 系统/
```

OpenAPI 0.14.0、Java 实现和正式数据库已经切换到 V3.1。Java 代码使用相同的模块目录，17 个自动化场景覆盖 33 个接口契约及核心校验。接口统一挂载在 `/api` 前缀下，正式业务接口使用 Bearer 会话，并统一返回 `{ code, message, data }` 包体。
