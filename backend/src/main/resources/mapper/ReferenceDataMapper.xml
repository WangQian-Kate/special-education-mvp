<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.specialed.assistant.mapper.ReferenceDataMapper">
    <select id="existsStudent" resultType="boolean">
        SELECT COUNT(*) > 0 FROM student WHERE id = #{id}
    </select>
    <select id="existsUser" resultType="boolean">
        SELECT COUNT(*) > 0 FROM app_user WHERE id = #{id}
    </select>
    <select id="existsBinding" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM app_user_student
        WHERE user_id = #{userId} AND student_id = #{studentId}
    </select>
    <select id="existsCourse" resultType="boolean">
        SELECT COUNT(*) > 0 FROM course_type WHERE code = #{code}
    </select>
    <select id="existsEnvironment" resultType="boolean">
        SELECT COUNT(*) > 0 FROM environment_type WHERE code = #{code}
    </select>
    <select id="existsAntecedent" resultType="boolean">
        SELECT COUNT(*) > 0 FROM antecedent_type WHERE code = #{code}
    </select>
    <select id="existsBehavior" resultType="boolean">
        SELECT COUNT(*) > 0 FROM behavior_type WHERE code = #{code}
    </select>
    <select id="existsConsequence" resultType="boolean">
        SELECT COUNT(*) > 0 FROM consequence_type WHERE code = #{code}
    </select>
    <select id="existsAssistance" resultType="boolean">
        SELECT COUNT(*) > 0 FROM assistance_type WHERE code = #{code}
    </select>
</mapper>
