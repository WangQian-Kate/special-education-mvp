# 拉取启动方法

> **给 Claude Code 看的启动流程**：读取本文档后，按顺序执行以下步骤。遇到需要用户输入的地方（如 API Key），**必须先询问用户再继续**。

---

## Claude Code 启动步骤

### 1. 检查 JDK

```bash
java -version
```

版本低于 17 → 告知用户"需要 JDK 17+，请安装后设 JAVA_HOME"，等待确认后再继续。

### 2. 查找 JDK 21+

```bash
ls "C:\Users\$env:USERNAME\.jdks\" 2>/dev/null || ls /usr/lib/jvm/ 2>/dev/null || echo "请手动指定 JDK 路径"
```

找到后设置 `JAVA_HOME`。

### 3. 检查 MySQL

```bash
mysql -u root -proot -e "SELECT 1" 2>/dev/null && echo "MySQL OK" || echo "需要确认 MySQL 密码"
```

如果 root 密码不是 `root`，询问用户。然后：

```sql
CREATE DATABASE IF NOT EXISTS special_ed_assistant CHARACTER SET utf8mb4;
CREATE USER IF NOT EXISTS 'special_ed_app'@'localhost' IDENTIFIED BY 'special_ed_2024';
GRANT ALL PRIVILEGES ON special_ed_assistant.* TO 'special_ed_app'@'localhost';
FLUSH PRIVILEGES;
```

### 4. 导入表结构和种子

```bash
mysql -u root -p special_ed_assistant < backend-wt/backend/sql/schema.sql
mysql -u root -p special_ed_assistant < backend-wt/backend/sql/seed.sql
```

### 5. 🔑 询问智谱 API Key

**必须在此步骤询问用户**：

> "需要智谱清言 API Key 才能使用 AI 分析功能。你有 Key 吗？如果有请粘贴给我；如果没有，AI 功能将使用模板报告（也能用）。  
> 获取地址：https://open.bigmodel.cn/"

- 用户提供了 Key → 写入 `backend-wt/backend/src/main/resources/application-dev.yml` 的 `api-key` 字段
- 用户没有 → 留空，启动后 AI 用模板报告

### 6. 启动后端

```bash
cd backend-wt/backend
$env:JAVA_HOME = "JDK路径"
$env:DB_PASSWORD = "special_ed_2024"
./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
```

确认 `curl http://localhost:3000/api/health` 返回 `{"code":0}`。

### 7. 启动前端

告知用户：用微信开发者工具打开 `special-education-mvp` 目录，勾选"不校验合法域名"即可。

---

## 手动启动参考（以下为详细环境说明）

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| JDK | **17 或以上**（推荐 21） | 项目 pom.xml 使用 Spring Boot 4.1 |
| MySQL | 8.0 | 需创建数据库并导入种子数据 |
| Maven | 自带（mvnw） | 无需额外安装 |
| 微信开发者工具 | 最新版 | 导入 `special-education-mvp` 目录 |

### JDK 检查与设置

```bash
java -version
# 如果版本低于 17，需要安装 JDK 21 并设置环境变量：
# Windows: 系统属性 → 环境变量 → JAVA_HOME = C:\你的JDK21路径
# Mac/Linux: export JAVA_HOME=/你的JDK21路径
```

### MySQL 初始化（首次）

用 root 账号执行：

```sql
CREATE DATABASE IF NOT EXISTS special_ed_assistant CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
CREATE USER IF NOT EXISTS 'special_ed_app'@'localhost' IDENTIFIED BY 'special_ed_2024';
GRANT ALL PRIVILEGES ON special_ed_assistant.* TO 'special_ed_app'@'localhost';
FLUSH PRIVILEGES;
```

然后导入表结构和种子数据：
```bash
cd backend-wt/backend
mysql -u root -p special_ed_assistant < sql/schema.sql
mysql -u root -p special_ed_assistant < sql/seed.sql
```

---

## 后端启动

```bash
cd backend-wt/backend

# 确保 JAVA_HOME 指向 JDK 17+
java -version

# 启动（默认 dev 环境，端口 3000）
./mvnw spring-boot:run
# Windows: mvnw.cmd spring-boot:run
```

启动成功标志：
```
Started AssistantApplication in X seconds
```

验证：
```bash
curl http://localhost:3000/api/health
# → {"code":0,"message":"ok","data":{"status":"UP"}}
```

### 后端配置说明

配置在 `application-dev.yml`，无需修改直接可用：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/special_ed_assistant
    username: special_ed_app
    password: special_ed_2024    # 已写死，无需设环境变量

ai:
  base-url: https://open.bigmodel.cn/api/paas/v4/chat/completions
  api-key: ebe462b21dd841ca8c49df5b525601b5.gnW6IV5tcfOA5yAy   # 智谱 GLM-4
  model: glm-4-flash
```

**注意**：如果数据库密码不同，修改 `password` 字段。AI Key 如需更换，修改 `api-key` 字段。

---

## 前端启动

1. 打开**微信开发者工具**
2. 导入项目 → 选择 `special-education-mvp` 目录
3. AppID 使用 `wx85f5b6f85abcf1578`（或你自己的测试号）
4. 在开发者工具「详情」→「本地设置」勾选「不校验合法域名」

API 地址在 `miniprogram/utils/request.js`：
```js
const BASE_URLS = {
  local: 'http://localhost:3000/api',     // 开发者工具用
  tunnel: 'https://你的隧道地址/api'       // 真机联调用
};
```

---

## AI 模块（智谱清言）

学生评估页 → 周报/月报 → AI 智能分析。后端 `POST /ai/report` 调用智谱 GLM-4 生成报告。

**需要自己申请智谱 API Key：**

1. 打开 [智谱AI开放平台](https://open.bigmodel.cn/)，注册/登录
2. 进入「API Keys」页面，复制 Key
3. 启动后端时传入：

   ```bash
   # Windows CMD
   set AI_API_KEY=你的key
   mvnw spring-boot:run

   # Windows PowerShell
   $env:AI_API_KEY="你的key"
   ./mvnw spring-boot:run

   # Mac / Linux
   AI_API_KEY=你的key ./mvnw spring-boot:run
   ```

4. 或在 `application-dev.yml` 中直接填写：
   ```yaml
   ai:
     api-key: 你的key   # 替换这里
   ```

- **未配置 Key**：自动退回模板报告（也能用，只是分析较简单）
- **已配置 Key**：调智谱 GLM-4 生成真 AI 分析

---

## 真机联调

1. 运行隧道脚本：
   ```bash
   cd scripts/tunnel
   configure-cpolar.cmd     # 首次配置
   start-cpolar-tunnel.cmd  # 启动隧道
   ```

2. 复制输出的 HTTPS 地址（如 `https://xxxx.cpolar.cn/api`）

3. 修改 `miniprogram/utils/request.js`：
   ```js
   const ENV = 'tunnel';  // 改为 tunnel
   ```
   并把 `tunnel` 地址改为上一步复制的地址

4. 微信开发者工具 → 预览 → 扫码在手机上打开

---

## 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| `BUILD FAILURE` / `release 25 not supported` | JDK 版本太低 | 设 `JAVA_HOME` 到 JDK 21 |
| `Access denied for user` | 数据库密码不对 | 在 `application-dev.yml` 修改 |
| `Table doesn't exist` | 没导入 SQL | 执行 schema.sql + seed.sql |
| 前端 404 / 网络异常 | 后端没启动 | 确认 `curl localhost:3000/api/health` 返回 UP |
| 真机无法连接 | 没有隧道 | 启动 cpolar 隧道并改 request.js |
| AI 返回模板报告 | 未配智谱 Key | 申请智谱 Key 后设置 `AI_API_KEY` 环境变量 |
