# 成员 B 学生评估趋势统计验收报告

> 日期：2026-07-18
> 任务：处理第一小阶段遗留的行为趋势统计
> 结论：已完成后端实现和自动化验证。

## 完成内容

- 扩展 `GET /api/student-evaluation/statistics`，保持原有 `totalCount` 和 `items[].count` 字段兼容。
- 新增当前周期概况：观察课程、行为记录、ABC 记录和备注数量。
- 新增上一周期日期、上一周期行为次数、变化百分比和趋势方向。
- 日报与前一天比较，周报与上一自然周比较，月报与上一自然月比较。
- 当前或上一周期至少出现过一次的行为都会进入趋势列表。
- 上升返回 `UP`，下降返回 `DOWN`，不变返回 `STABLE`；前端分别显示红、绿、灰。
- 上期为 0、本期大于 0 时返回 `changePercent: null` 和 `trendDirection: UP`。

## 统计口径

| 前端字段 | 后端口径 |
|---|---|
| 观察课程 | 当前周期课堂记录数量 |
| 行为记录 | 当前周期行为记录总数 |
| ABC 记录 | `detailSaved = true` 的行为记录数 |
| 备注 | 填写了本周期行为备注的课堂记录数 |

## 验证结果

已新增周报环比集成测试，覆盖概况统计、下降 50%、上期为 0 的上升行为以及周期日期计算。

```text
Tests run: 6, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

整条课堂记录删除接口按已确认方案暂不增加，继续保留删除单次行为记录的现有能力。
