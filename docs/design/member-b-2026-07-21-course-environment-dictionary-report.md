# 成员 B 课程与环境字典更新报告

## 完成内容

- 课程字典收敛为前端确认的 16 项稳定编码。
- 环境字典收敛为前端确认的 7 项稳定编码。
- “劳动”“综合”“地方”不设独立课程项，统一迁移和提交为 `OTHER`。
- 新增幂等迁移 `migrate-v2.5-course-environment-dictionaries.sql`，正式本地库升级到 `2.5.0`。
- OpenAPI 升级到 `0.10.0`，`courseCode` 和 `environmentCode` 改为固定枚举。
- 更新中文接口文档、数据字典、数据模型、迁移说明，并生成两份飞书手动同步稿。

## 历史数据规则

- 不在新列表中的课程统一更新为 `OTHER`，原课程名称保存在 `course_other_description`。
- 旧课程的行为卡配置合并到 `OTHER`，重复配置按联合主键去重。
- 不在新列表中的环境统一更新为 `OTHER`，原环境名称保存在 `environment_other_description`。
- 迁移完成后删除旧字典项，后端会拒绝再次提交旧 code。

## 验证结果

| 验收项 | 结果 |
|---|---|
| 正式库迁移前备份 | `special_ed_assistant-v24-before-dictionaries-20260721-181750.sql` |
| V2.5 迁移连续执行两次 | 通过 |
| `seed.sql` 连续执行两次 | 通过 |
| 课程字典精确 16 项 | 通过 |
| 环境字典精确 7 项 | 通过 |
| 劳动/综合/地方迁移到 `OTHER` 并保留名称 | 通过 |
| 旧环境迁移到 `OTHER` 并保留名称 | 通过 |
| 临时历史数据清理 | 通过，剩余 0 行 |
| 数据库集成测试 | 6 个场景全部通过 |
| OpenAPI 校验 | 通过，保留 3 个既有非阻塞警告 |

正式数据库业务表未留下测试记录。
