# 特教课堂助手 · 微信小程序前端开发流程

> 依据原型：`../素材/special-ed-assistan_v2.html`（v2）
> 目标：完成可通过微信审核、正式上线的小程序前端
> 技术路线：**原生小程序**（WXML/WXSS/JS）+ echarts-for-weixin，不引入跨端框架
> 当前代码基线：`miniprogram/` 下已有 index/record/report/statistics/students 五个骨架页，**与原型结构不一致，将按本文档重构**

---

## 一、原型 → 小程序页面映射

原型是单 HTML 多"屏"切换，落地到小程序需拆成独立页面 + tabBar：

| 原型模块 | 小程序页面路径 | 类型 | 说明 |
|---|---|---|---|
| 1. 启动页 | 无需单独页面 | — | 小程序自带启动加载，逻辑放 `app.js` 冷启动判断 |
| 2. 微信登录页 | `pages/login/login` | 普通页 | wx.login + 授权按钮 |
| 3. 完善个人信息 | `pages/onboarding/teacher/teacher` | 普通页 | 教师姓名/学校/职务；"我是家长"分支 |
| 3.5 孩子信息页 | `pages/onboarding/child/child` | 普通页 | 障碍类型多选、能力等级单选组 |
| TAB1 随班记录 | `pages/records/records` | **tabBar 页** | 内含 日/周/月 三个子视图 |
| — ABC 行为详情弹窗 | `components/abc-modal/` | 自定义组件 | 半屏弹窗，日记录页调用 |
| TAB2 训练计划 | `pages/plans/plans` | **tabBar 页** | 分类 tab + 搜索 + 状态筛选 + 可编辑表格 |
| TAB3 学生评估 | `pages/assessment/assessment` | **tabBar 页** | 日报/周报/月报/学期报告四个子视图 |
| TAB4 我的 | `pages/profile/profile` | **tabBar 页** | 学生切换、信息管理、退出登录 |
| — 学生信息管理 | `pages/profile/students/students` | 二级页 | 学生列表、增删改 |
| — 个人资料修改 | `pages/profile/edit/edit` | 二级页 | |

**重构动作**：现有 `pages/index、record、report、statistics、students` 按上表迁移/改名，`app.json` 的 `pages` 与 `tabBar` 同步更新。tabBar 4 项：随班记录 / 训练计划 / 学生评估 / 我的（图标需准备 81×81px 的选中/未选中两套 PNG，原型用的 mingcute 图标可导出）。

---

## 二、技术选型与约定

| 事项 | 决策 | 原因 |
|---|---|---|
| 框架 | 原生小程序 | 项目已是原生结构，页面量少（约 10 页），无跨端需求 |
| UI 库 | 不引入，手写组件 | 原型是定制视觉（卡片圆角 14px、主色 #5B9BD5），UI 库反而增加改样式成本 |
| 图表 | [echarts-for-weixin](https://github.com/ecomfe/echarts-for-weixin)（ec-canvas） | 评估页的柱状图/趋势图/雷达图；简单的横向进度条直接用 view 宽度百分比实现（同原型） |
| 状态管理 | `app.globalData` + 本地封装 `store.js` | 全局仅需：登录态、当前教师、当前学生；不需要 mobx |
| 请求层 | 自封装 `utils/request.js`（Promise 化 wx.request） | 统一 baseURL、身份注入、错误 toast |
| 数据层 | `api/` 目录按模块拆分，**先接 mock 后换真接口** | 前端不被后端进度阻塞 |
| 样式 | WXSS + 设计令牌（app.wxss 定义 CSS 变量） | 原型的 Tailwind 类不可用，需translate 成设计令牌 |
| 尺寸单位 | rpx（原型 375px 稿 → 1px = 2rpx） | |
| 代码检查 | 已有 ESLint，补充提交前 `npm run lint` | |

### 设计令牌（从原型提取，写入 `app.wxss`）

```css
page {
  --color-primary: #5B9BD5;      /* 主色 */
  --color-bg: #EEF0F4;           /* 页面背景 */
  --color-success: #4ade80;      /* 正向行为绿 */
  --color-danger: #f87171;       /* 问题行为红 */
  --color-warning: #fb923c;      /* 暂停橙 */
  --color-text: #374151;
  --color-text-sub: #999;
  --radius-card: 28rpx;          /* 卡片 14px */
  --radius-modal: 56rpx;         /* 半屏弹窗 28px */
}
```

通用样式类：`.card`（白底圆角 14px 内边距 14px）、`.card-sm`、`.sec-title`（11px 灰色小节标题）、`.btn-active`（按压缩放反馈，用 hover-class 实现）。

---

## 三、目录结构（目标形态）

```
miniprogram/
├── app.js / app.json / app.wxss
├── pages/
│   ├── login/
│   ├── onboarding/teacher/  onboarding/child/
│   ├── records/            # TAB1（day/week/month 用模板或组件拆分）
│   ├── plans/              # TAB2
│   ├── assessment/         # TAB3（daily/weekly/monthly/semester）
│   └── profile/            # TAB4 + students/ edit/ 二级页
├── components/
│   ├── abc-modal/          # ABC 行为记录半屏弹窗
│   ├── behavior-counter/   # 行为计数卡片（+/- 计数）
│   ├── nav-date/           # 日/周/月切换的日期导航条
│   ├── stat-grid/          # 4 格概况统计（周/月概况复用）
│   ├── progress-bar/       # 横向占比条（行为趋势/维度评估复用）
│   ├── ai-card/            # AI 分析卡片（蓝/橙/绿三色提示）
│   ├── calendar/           # 月视图日历
│   └── ec-canvas/          # echarts 组件（第三方拷入）
├── api/
│   ├── auth.js  student.js  record.js  plan.js  report.js
├── mock/                   # 原型内嵌数据抽取为 JSON（behaviors、planCategories、周/月数据）
├── utils/
│   ├── request.js  store.js  date.js  constants.js
└── assets/tabbar/          # tabBar 图标
```

`utils/constants.js` 收口原型中的所有枚举：课程列表、环境列表、行为类型、障碍类型（ASD/ID/ADHD…11 项）、行为功能（获得注意/获得物品/逃避/感觉刺激）、辅助方式（刺激内 3 项 + 刺激外 4 项）、训练状态（未开始/进行中/已完成/暂停）、能力等级（A–F）、阶段（1–3）。

---

## 四、核心数据模型（前后端对齐用）

开工前和后端确认以下实体，前端按此写 mock：

```
Teacher   { id, openid, name, school, role(特教教师|康复师|班主任), avatar }
Student   { id, name, age, gender, className, disabilityTypes[], abilities{language, social, selfCare}, note }
Course    { date, subject, environment, cycle(15|40|60|480|自定义min), note }        // 一节观察课
Behavior  { id, name, positive:boolean }                                             // 行为字典
BehaviorLog { id, courseId, behaviorId, time, count }                                // 计数记录
AbcRecord { id, behaviorLogId, time, duration, phase(行为环节), antecedent, behavior,
            consequence, function(attention|item|escape|sensory),
            prompts[{type, content}], promptResult }                                 // ABC 详录
Evaluation { date, dims{情绪行为, 社会适应, 社会交往, 自我管理, 语言理解表达, 专注力} } // 全天汇总教师评价
Plan      { id, category, title, levelInit(A-F), levelNow(A-F), phase(1-3),
            status(未开始|进行中|已完成|暂停) }
Report    { type(daily|weekly|monthly|semester), stats, trends, aiAnalysis[] }
```

---

## 五、MVP 分阶段开发流程（Day 2–13 · 仅前端职责）

> 当前目标是 **MVP**，不是正式上线版：合规资质、正式 appid、隐私协议、导出 PDF 等推迟到 MVP 验证之后（参考附录 A/B 留档）。
> 本节只列**前端任务**；后端、测试/大模型同学的产出以「依赖」标注，作为每阶段的对接点。

### MVP 页面范围裁剪

| 保留（MVP 必做） | 简化 | 延后（MVP 不做） |
|---|---|---|
| 伪登录首页（影子老师按钮） | 周/月视图 → 先做当日记录列表 | 正式微信登录（阶段四视进度接入） |
| 随班记录·日视图（行为计数 + ABC 弹窗） | ABC 表单可减为：时间/时长/环节 + A/B/C + 行为功能 | 学期报告 |
| 行为列表展示（当日记录，倒序） | 「我的」→ 仅保留切换教师/学生入口 | 引导流程（教师/孩子信息表单）、导出/分享报告 |
| 训练计划 TAB（分类/搜索/状态筛选/行内编辑，先本地 mock） | 编辑仅改本地，阶段三接后端保存 | |
| 统计页（图表 + AI 分析卡片） | | 隐私合规弹窗、分包优化 |

### 阶段二 · 基础环境搭建（Day 2–3）

- [ ] 确认小程序项目可跑（现有骨架即可，MVP 阶段继续用 `touristappid`，不提审）
- [ ] 页面框架：`app.json` 注册 MVP 页面 + tabBar（先 3 个：记录 / 统计 / 我的）
- [ ] `app.wxss` 写入设计令牌与通用样式类（见第二节）
- [ ] 基础组件打底：`behavior-counter`、`stat-grid`、`ai-card`（`progress-bar` 视统计页方案决定）
- [ ] **ECharts 静态 Demo 先行**：拷入 `ec-canvas`，写死数据画一张行为频次柱状图，**真机扫码验证渲染**；同时看包体积（echarts 全量 ~1MB，需用[定制构建](https://echarts.apache.org/zh/builder.html)只保留 bar/line）。若真机渲染或体积不可接受 → 回退方案：view 宽度百分比进度条（原型同款实现，零依赖）
- [ ] **白名单伪登录**：首页放 2–3 个影子老师角色按钮 → 点击把 `teacherId` 写入 store + `wx.setStorageSync` → `utils/request.js` 统一在请求头携带（如 `X-Teacher-Id`），**不做 token 校验**；后续换正式登录时只动 login 页和 request 头两处
- [ ] `utils/request.js`：Promise 封装 + baseURL 可配置（本地 / 内网穿透地址一键切换）+ 统一错误 toast
- [ ] `mock/` 准备：从原型抽 behaviors 字典等基础数据

**阶段目标（对接点）**：调通后端健康检查/测试接口，**前后端基础通信测试通过**（开发者工具中先关闭"域名校验"）。
**依赖**：后端项目初始化完成、给出一个可调的测试接口。



### 阶段三 · 核心功能开发（Day 4–7）

- [ ] **ABC 行为记录页**：行为计数网格（+/- 即时响应、计数动画、正向行为绿色）；点击卡片弹出 `abc-modal` 半屏弹窗（MVP 简化字段：发生时间、持续时间、行为环节、A/B/C 三段文本、行为功能）
- [ ] **行为列表展示**：当日记录按时间倒序列表（区分普通计数与 ABC 详录），下拉刷新，空态提示
- [ ] **数据请求功能**：对接后端三个接口——行为新增、行为查询、数据统计；`api/record.js` 保留 `USE_MOCK` 开关
- [ ] 真机联调：baseURL 切到内网穿透（cpolar/Ngrok）HTTPS 地址后扫码测试；提交失败要有明确 toast + 可重试
- [ ] 统计接口的数据先在统计页用 Demo 图表接上（真实数据进图）

**阶段目标**：跑通全链路——**小程序填写行为 → 后端接收 → 数据库保存 → 列表/统计页展示**。
**依赖**：后端三个接口 + 内网穿透（或云服务器）地址；测试同学的接口测试与数据验证反馈。

### 阶段四 · 系统完善与功能优化（Day 8–10）—— 交社会学院老师试用，按反馈修改

- [ ] **页面美化**：对照原型统一卡片、配色、间距、字号；tabBar 图标补齐
- [ ] **图表展示**：统计页接入真实统计接口（行为频次柱状图；趋势对比用进度条 + 环比箭头，同原型）
- [ ] **AI 分析卡片**：展示后端大模型生成的干预建议（`ai-card` 蓝=趋势/橙=预警/绿=建议），处理三种状态：生成中 loading、成功、失败（失败给重试按钮，不阻塞页面其他内容）
- [ ] **体验优化**：请求 loading、空态/错误态统一、计数连点防抖合并上报、键盘遮挡输入框处理
- [ ] （时间充裕才做）**替换正式微信登录**：前端 `wx.login` 拿 code → 后端 code2Session 换 session → request 头从 `X-Teacher-Id` 换 token；伪登录代码保留开关以便回退
- [ ] 现场记录老师反馈，按"阻塞使用 > 数据不对 > 体验"排优先级当天修

**依赖**：后端统计完善 + 大模型分析接口（Prompt 已由测试/分析同学调稳）。

### 阶段五 · 测试与版本冻结（Day 11–13）

- [ ] 开发者工具全流程回归：伪登录（或正式登录）→ 计数 → ABC 详录 → 列表 → 统计/AI 分析
- [ ] **手机扫码预览真机测试**：iOS + Android 至少各一台；重点验证 ec-canvas 渲染、半屏弹窗滚动、键盘弹起
- [ ] 修复 Bug、交互打磨（弹窗滚动穿透、小屏适配、返回逻辑）
- [ ] 版本冻结前清理：删调试代码与 console、`USE_MOCK` 关闭、baseURL 锁定、`npm run lint` 通过
- [ ] git 合入 develop → main，打 tag **`v0.1.0-mvp`**

**MVP 自测清单（前端）**
- [ ] 影子老师切换后，记录/统计数据随之切换
- [ ] 行为计数快速连点 20 次，服务端数据准确
- [ ] ABC 保存后立刻出现在列表；杀进程重进数据仍在（服务端拉取）
- [ ] 断网提交有失败提示，恢复后可重试成功
- [ ] 图表在 iOS/Android 真机均正常渲染
- [ ] AI 分析失败时页面其余部分不受影响

---

## 六、里程碑总览（MVP）

| 里程碑 | 天数 | 前端交付物 | 通过标准 |
|---|---|---|---|
| M1 环境就绪 | Day 2–3 | 页面框架 + 基础组件 + 伪登录 + 图表真机 Demo | 前后端基础通信测试通过；图表真机可渲染 |
| M2 核心链路 | Day 4–7 | ABC 记录页 + 行为列表 + 接口对接 | 填写→接收→入库→展示 全链路走通 |
| M3 可试用版 | Day 8–10 | 美化 + 真实图表 + AI 卡片（+正式登录，选做） | 社会学院老师完成一轮试用，反馈已分级处理 |
| M4 MVP 冻结 | Day 11–13 | Bug 修复 + 真机回归 + 版本冻结 | 自测清单全过，打 tag `v0.1.0-mvp` |

风险提前量：**ECharts 真机渲染（Day 2 就验证）** 和 **内网穿透 HTTPS 可用性（Day 4 前要拿到地址）** 是前端进度的两个外部卡点，出问题当天升级同步，不拖到集成日。

---

## 附录 A · 提审前自测清单（正式上线阶段用，MVP 不要求）

- [ ] 新用户完整注册流程（含跳过分支）无卡死
- [ ] 四 tab 切换、每页下拉刷新、返回逻辑正常
- [ ] 行为计数快速连点 20 次数据准确；断网计数恢复后补传
- [ ] ABC 记录保存后在周/月视图可见
- [ ] 训练计划行内编辑 → 杀进程重进数据仍在
- [ ] 报告导出/分享在 iOS + Android 真机均可用
- [ ] 切换学生后所有页面数据正确刷新
- [ ] 退出登录后无残留数据；重新登录数据恢复
- [ ] 无 `wx.getUserProfile` 等废弃 API；无 console 报错
- [ ] 隐私弹窗在首次进入时出现，拒绝后功能受限但不闪退

## 附录 B · 原型与实现的差异决策记录

| 原型做法 | 小程序实现 | 原因 |
|---|---|---|
| 启动页 2s 动画 | 去掉，用小程序原生加载 | 小程序不允许自定义启动页拖时长 |
| 登录页展示头像昵称后授权 | MVP 用白名单伪登录；正式版用头像昵称填写能力 | getUserProfile 已废弃；MVP 优先跑通链路 |
| Tailwind 原子类 | WXSS 设计令牌 + 通用类 | 小程序不支持 Tailwind 运行时 |
| iconify 图标 | 导出 SVG→PNG 或使用 iconfont 字体 | 小程序无 iconify runtime |
| 表格 `<table>` | view 弹性布局模拟表格 | WXML 无 table 标签 |
| 弹窗内 select | picker / picker-view | WXML 无 select |

---

*本文档随开发推进更新：每完成一个阶段，勾选对应项并记录实际耗时与偏差。*
